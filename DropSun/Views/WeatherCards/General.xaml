<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DropSun.Views.WeatherCards.General"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DropSun.Views.WeatherCards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DropSun.Model.ViewModels"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <StackPanel>
            <TextBlock Text="{x:Bind vm:WeatherState.Instance.Forecast.Current.Temperature2M, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="{x:Bind vm:WeatherState.Instance.Forecast.Current.ApparentTemperature, Mode=OneWay}" Style="{StaticResource DisplayTextBlockStyle}"/>
        </StackPanel>
    </Grid>
</Page>
